kind: 'Changed'
body: 'refreshed `.github/copilot-instructions.md` so the repository structure lists the `.changes/unreleased/` chlog directory, the `code-review` skill, and the `claude-review.yaml` / `claude-mention.yaml` workflows, and the build section notes those callers delegate to `rios0rios0/pipelines`'
time: '2026-09-01T16:54:48.11682463Z'
